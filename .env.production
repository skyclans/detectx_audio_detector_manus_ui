# Production build env (Vite reads VITE_* at build time)
# Routes all client API calls through detectx.app (Cloudflare → Nginx → Uvicorn)
# instead of bypassing to RunPod proxy directly.
VITE_DETECTX_API_URL=https://detectx.app
VITE_API_URL=https://detectx.app/api
